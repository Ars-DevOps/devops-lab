name: check-storoj



on: 
  push: 
    branches:
      - main


jobs:
  my_testing:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v1
      - name: test
        run: ./storoj.sh

  server_log:
    runs-on: ubuntu-latest
    needs: [my_testing] 
    steps:
       - name: cat
         run: cat server.log
         
     

  
      
